@PACKAGE_INIT@

# Find dependencies that Soar requires
# Use CONFIG mode to ensure we find Conan-provided packages
include(CMakeFindDependencyMacro)
find_dependency(SQLite3 REQUIRED CONFIG)
find_dependency(Boost REQUIRED CONFIG)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/SoarTargets.cmake")
